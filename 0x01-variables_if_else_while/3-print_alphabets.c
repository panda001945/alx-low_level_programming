#include <stdio.h>
/**
 * main - print if the number is positive or negative
 *
 *description: using the main function
 *this program prints "programming is positive, zero ot negative
 *retun: 0
 */
int main(void)
{
         char ch;

         for (ch = 'a' ; ch <= 'z' ; ch++)
}
        putchar(ch);
}
for (ch = 'A' ; ch <= 'Z" ; ch++)
{
        putchar(ch);
}
putchar('\n');
return (0);
}
